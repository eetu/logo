node_modules
dist
.DS_Store
_*.png
*.log

# Yarn (berry) — vendor the release binary, ignore the rest
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks

# raw generated sources kept locally for re-keying, not committed
src/assets/*_org.*
